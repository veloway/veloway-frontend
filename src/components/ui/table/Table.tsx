
type Data = Object[];

interface TableProps {
    title: string;
    columns: string[];
    data: Data;
}

export default function Table({}:TableProps) {

    return (
        <div>
            
        </div>
    )
}
